'use client';

import { LoginForm } from '../../component/LoginForm';

export default function LoginFrom() {
  return (
    <html>
      <body>
        <main>
          <LoginForm />
        </main>
      </body>
    </html>

  );
}
